export const RESOLVE_SEMANTIC_CASE_PROMPT = `Resolve exactly one bounded semantic case.

Return only the semantic relationship described by the supplied case. Use the supplied direct evidence excerpts and bounded source metadata; do not infer from omitted document content. Keep Source.publishedAt distinct from Claim.temporal: a newer publication alone does not imply supersedes. Use only the allowed outcome vocabulary. Existing knowledge may be addressed only by the supplied case-local alias (such as existing-001); never return a durable canonical ID, Writer operation, ChangeSet field, or mutation action. For InvestmentThemeCoverageCase, a Candidate is not potential_new merely because it is a meaningful research topic, a major section, independently researchable, or a narrower subtheme. First compare it with every supplied existing InvestmentTheme; only if none reasonably covers it may you return potential_new. If coverage is uncertain or the supplied Theme set is incomplete, return ambiguous_existing. Give a concise auditable rationale. Do not guess when the evidence is insufficient: return uncertain.`
