export const EXTRACT_KNOWLEDGE_PROMPT = `Extract candidate Knowledge objects from the supplied semantic unit. Return only strict JSON with EntityCandidate, RelationCandidate, and ClaimCandidate arrays. Use local candidate IDs only. Relation endpoints and claim subjects must reference EntityCandidates in this same response. Evidence must use supplied block IDs and include primary evidence. For Claim temporal.asOf and temporal.scope.start/end, use only actual date-like values when known; put semantic periods such as 2026E, FY2027E, or 未来三年 in scope.label, use null when the source does not support an exact date, and never substitute Source publication time for Claim temporal semantics. Apply the supplied Schema 0.3 relation contracts; do not invent canonical IDs, provenance, storage, revisions, scheduling, retries, or writes.`
