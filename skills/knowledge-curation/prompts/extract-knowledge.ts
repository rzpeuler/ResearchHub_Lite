export const EXTRACT_KNOWLEDGE_PROMPT = `Extract candidate Knowledge objects from the supplied semantic unit. Return only strict JSON with EntityCandidate, RelationCandidate, and ClaimCandidate arrays. Use local candidate IDs only. Relation endpoints and claim subjects must reference EntityCandidates in this same response. Evidence must use supplied block IDs and include primary evidence. For Company Entities, use the projected Company semanticFields contract: ticker, exchange, and legalName are structured fields. If a source writes a clear trailing securities decoration such as 上海新阳（300236.SZ） or 中巨芯(688549.SH), output the clean Company display name and put ticker and normalized exchange in semanticFields; do not keep the securities code in name. If a source clearly uses 中文名（ASCII English Name）, prefer the Chinese display name and put the ASCII label in aliases. Do not decompose ambiguous Chinese parenthetical qualifiers such as 彤程新材（北京科华）, and do not split slash labels such as AXT / 北京通美 by deterministic rule. For Claim temporal.asOf and temporal.scope.start/end, use only actual date-like values when known; put semantic periods such as 2026E, FY2027E, or 未来三年 in scope.label, use null when the source does not support an exact date, and never substitute Source publication time for Claim temporal semantics. For Relation attributes, emit only keys and nested fields declared by the supplied contract, use only its declared enum and numeric ranges, and omit optional attributes unsupported by the evidence; relations without custom attributes must omit them or use an empty object. Apply the supplied Schema 0.3 relation contracts; do not invent canonical IDs, provenance, storage, revisions, scheduling, retries, or writes.`
