export const UNDERSTAND_AND_PLAN_PROMPT = `You are performing semantic understanding and planning for a research document. Produce only strict JSON matching the supplied output contract. Assess the source, research scope, topics, entity mentions, conclusions, section semantics, dependencies, themes, uncertainty, and proposed semantic extraction units.

The ExtractionPlanProposal is EXHAUSTIVE. Every document Block must end in exactly one of: primary responsibility of one ExtractionUnit, or explicit excludedRefs. ContextRefs do not satisfy primary coverage. A Block must not be primary-owned by more than one Unit. excludedRefs must contain material intentionally not requiring semantic extraction; do not omit material implicitly. Use Section refs when an entire Section can share one responsibility, and Block refs for exceptions or partial Sections. Do not create arbitrary tiny Units merely to mechanically cover every block. Keep semantic grouping topic/dependency driven. Do not exclude useful research content merely to satisfy token budgets.

Return a COMPLETE ReportMap plus a COMPLETE ExtractionPlanProposal, including excludedRefs even when it is empty. Evidence references must be supplied block or section references. Do not invent canonical Knowledge IDs. Do not decide workflow nodes, budgets, retries, storage, revisions, ChangeSets, or acceptance.`

export const PLAN_REPAIR_PROMPT = `The previous complete semantic plan failed deterministic plan admissibility. Deterministic feedback is authoritative about the structural failure. Repair the semantic grouping, primary ownership, and explicit exclusions while preserving valid semantic understanding where possible. Return a COMPLETE replacement ReportMap plus ExtractionPlanProposal, not a patch. Do not bypass or argue with the validator, do not invent canonical IDs, and do not decide whether another retry is allowed.`
