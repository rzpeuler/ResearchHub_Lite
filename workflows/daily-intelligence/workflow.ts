import { join, resolve } from 'node:path'
import { hashKnowledgeObject } from '../../knowledge/storage/canonical-hash.ts'
import type { DailyBriefReport, DailyIntelligenceInput, DailyIntelligenceResult, DailyProviderOutcome, DailyResearchSignal, ResearchChangeAssessment, DailySignalCluster } from '../../plugins/daily-intelligence/contracts.ts'
import { FileDailyBriefStore } from '../../plugins/daily-intelligence/brief-store.ts'
import { deduplicateDailySignals, clusterDailySignals, rankDailySignals } from '../../plugins/daily-intelligence/signal-intelligence.ts'
import { DailyIntelligenceSignalEnrichmentSkill } from '../../skills/daily-intelligence/enrichment.ts'
import { DailyBriefSynthesisSkill, type DailySynthesisProposal } from '../../skills/daily-intelligence/synthesis.ts'
import { validateResearchReport, writeResearchReport, type ResearchReport } from '../../app/services/research-report.ts'
import { KnowledgeBaseRegistry } from '../../knowledge/registry/registry.ts'
import { KnowledgeProductionGateway } from '../../knowledge/production/gateway.ts'
import type { NormalizedResearchSource } from '../../plugins/research-acquisition/contracts.ts'
import type { SemanticProductionProposal } from '../../knowledge/production/contracts.ts'

const safeId = /^[A-Za-z0-9][A-Za-z0-9._-]*$/
const abort = (signal?: AbortSignal) => { if (signal?.aborted) throw new Error('WORKFLOW_CANCELLED') }
const providerName = (plugin: { name: string }) => plugin.name.includes('official') ? 'cninfo' : plugin.name.replace(/-research-acquisition$/, '').replace(/^web-research-/, '')
const errorStatus = (message: string): DailyProviderOutcome['status'] => /401|403|429|captcha|login|authentication|blocked/i.test(message) ? 'blocked' : 'failed'
function makeOutcome(provider: string, status: DailyProviderOutcome['status'], c: Counts, diagnostics: string[]): DailyProviderOutcome { return { provider, status, attempted: true, succeeded: status === 'succeeded', empty: status === 'empty', blocked: status === 'blocked', failed: status === 'failed', usable: c.usable, diagnostics, discoveredCount: c.discovered, fetchSucceededCount: c.fetched, normalizeSucceededCount: c.normalized, emptyCount: c.empty, blockedCount: c.blocked, failedCount: c.failed } }
interface Counts { discovered: number; fetched: number; normalized: number; usable: number; empty: number; blocked: number; failed: number }

export async function runDailyIntelligence(input: DailyIntelligenceInput): Promise<DailyIntelligenceResult> {
  if (!safeId.test(input.workflowRunId) || !/^\d{4}-\d{2}-\d{2}$/.test(input.tradeDate)) return failure(input, 'failed', ['workflowRunId/tradeDate is invalid'])
  const now = input.now ?? (() => new Date().toISOString()); const asOf = input.asOf ?? now(); const store = new FileDailyBriefStore(input.briefRoot); const id = `daily-${input.briefType}-${input.tradeDate}`; const prior = await store.get(id)
  const calendar = input.calendar ? await input.calendar.isTradingDay(input.tradeDate) : { isTradingDay: true, calendarConfidence: input.calendarConfidence ?? 'fallback' as const }
  if (!calendar.isTradingDay) return { ...failure(input, 'completed', ['Non-trading day: acquisition skipped']), errors: ['Non-trading day: acquisition skipped'] }
  if (prior && !input.forceRefresh) return { workflowRunId: input.workflowRunId, status: 'already_completed', brief: prior, reportPath: join(input.reportRoot, `${id}.md`), signals: [], clusters: [], assessments: [], providerOutcomes: prior.providerOutcomes, proposalCount: 0, committedKnowledgeRefs: prior.committedKnowledgeRefs, reviewCaseCount: prior.reviewCaseCount, errors: [] }
  try {
    const signals: DailyResearchSignal[] = []; const sources: NormalizedResearchSource[] = []; const outcomes: DailyProviderOutcome[] = []
    for (const plugin of input.providers) { abort(input.signal); const counts: Counts = { discovered: 0, fetched: 0, normalized: 0, usable: 0, empty: 0, blocked: 0, failed: 0 }; const diagnostics: string[] = []; const broad = (plugin as { dailyScope?: string }).dailyScope === 'broad'; const scopes = broad ? [undefined] : input.watchlist.companies
      try { for (const company of scopes) { abort(input.signal); const candidates = await plugin.discover({ company: company ?? { symbol: 'BROAD_SCOPE' }, asOf, limitPerKind: 10 }); counts.discovered += candidates.length; if (!candidates.length) { counts.empty++; continue } for (const candidate of candidates) { try { const fetched = await plugin.fetch(candidate); if (!fetched.content.trim() && !fetched.rawBytes?.byteLength) { counts.empty++; continue } counts.fetched++; const normalized = await plugin.normalize(fetched); if (!normalized.content.trim()) { counts.empty++; continue } counts.normalized++; sources.push(normalized); const symbol = typeof candidate.metadata?.companySymbol === 'string' && candidate.metadata.companySymbol !== 'BROAD_SCOPE' ? candidate.metadata.companySymbol : undefined; const entities = symbol ? [symbol] : input.watchlist.companies.filter((item) => normalized.title.includes(item.name ?? item.symbol)).map((item) => item.symbol); const kind: DailyResearchSignal['kind'] = candidate.kind === 'official_disclosure' ? 'announcement' : /institution|analyst|broker/i.test(providerName(plugin)) ? 'institutional_view' : /community|guba|xueqiu/i.test(providerName(plugin)) ? 'community' : candidate.kind === 'structured_data' ? 'market' : 'news'; const category = classify(candidate.title, kind); const signal: DailyResearchSignal = { signalId: `signal-${hashKnowledgeObject({ provider: providerName(plugin), candidateId: candidate.candidateId, contentHash: normalized.contentHash }).slice(7, 27)}`, kind, category, provider: providerName(plugin), sourceAccountRef: typeof candidate.metadata?.sourceAccountRef === 'string' ? candidate.metadata.sourceAccountRef : providerName(plugin), ...(candidate.publishedAt ? { publishedAt: candidate.publishedAt, temporalConfidence: 'source' as const } : { temporalConfidence: 'unknown' as const }), discoveredAt: normalized.retrievedAt, source: candidate, entities, themes: [...new Set([...(candidate.metadata?.focusTags as string[] ?? []), ...input.watchlist.themes.filter((theme) => normalized.title.toLowerCase().includes(theme.toLowerCase()))])], title: normalized.title, contentRef: normalized.canonicalUrl, contentHash: normalized.contentHash, excerpt: normalized.content.slice(0, 500), relevance: entities.length ? 1 : 0.35, novelty: 1, importance: candidate.tier <= 2 ? 0.9 : 0.5, sourceTier: candidate.tier, clusterKey: eventKey(normalized.title, entities, category) }; signals.push(signal); counts.usable++ } catch (error) { counts.failed++; diagnostics.push(`${candidate.candidateId}: ${error instanceof Error ? error.message : String(error)}`) } } } outcomes.push(makeOutcome(providerName(plugin), counts.usable ? 'succeeded' : counts.failed ? errorStatus(diagnostics.join(' ')) : 'empty', counts, diagnostics)) } catch (error) { const message = error instanceof Error ? error.message : String(error); counts.failed++; diagnostics.push(message); outcomes.push(makeOutcome(providerName(plugin), errorStatus(message), counts, diagnostics)) } }
    const bounded = signals.slice(0, Math.max(1, Math.min(input.maxSignals ?? 500, 1000))); const deduped = deduplicateDailySignals(bounded); const previous = await input.signalStore.listWindow(`${shiftDate(input.tradeDate, -2)}T00:00:00.000Z`, `${input.tradeDate}T23:59:59.999Z`, 1000); const previousKeys = new Set(previous.map(identity)); const enriched = new DailyIntelligenceSignalEnrichmentSkill().enrichMany(deduped, input.watchlist.companies.map((item) => item.symbol), [...input.watchlist.themes, ...input.watchlist.industries], input.reasoningExecutor).map((signal) => ({ ...signal, novelty: previousKeys.has(identity(signal)) ? 0.2 : 1 })); const stored = await input.signalStore.appendMany(enriched); const clusters = clusterDailySignals(enriched); const projections = await projectExisting(input, clusters); const assessments = clusters.map((cluster) => assess(cluster, projections)); const ranked = rankDailySignals(enriched, asOf, input.topLimit ?? 20); const synthesis = await new DailyBriefSynthesisSkill().synthesize(input.briefType, ranked, clusters, assessments, input.reasoningExecutor); const knowledge = await projectKnowledge(input, sources, synthesis.proposals); const quality = makeQuality(enriched, deduped, clusters, ranked, synthesis.sections, assessments, stored.appended, synthesis.proposals.length, knowledge.claimCount, knowledge.rawClaimCount); const brief: DailyBriefReport = { reportId: id, briefType: input.briefType, tradeDate: input.tradeDate, asOf, timezone: 'Asia/Shanghai', revision: (prior?.revision ?? 0) + 1, workflowRunId: input.workflowRunId, generatedAt: now(), sections: synthesis.sections, topSignals: ranked, providerOutcomes: outcomes, quality, consensusStatement: 'Consensus unavailable unless explicitly supported by attributable point-in-time public observations; no paid or fabricated consensus is used.', knowledgeBaseRevision: knowledge.revision, committedKnowledgeRefs: knowledge.committedRefs, reviewCaseCount: knowledge.reviewCount, calendarConfidence: calendar.calendarConfidence }
    const report: ResearchReport = { reportId: id, reportType: 'daily_brief', subjectRefs: [...new Set(ranked.flatMap((signal) => signal.entities.filter((entity) => /^\d{6}$/.test(entity)).map((entity) => `entity:${entity}`)))], generatedAt: brief.generatedAt, asOf, workflowRunId: input.workflowRunId, knowledgeBaseRevision: knowledge.revision ?? 0, sourceRefs: [], claimRefs: [], methodology: 'Bounded item-level public acquisition, source-preserving normalization, deterministic deduplication/clustering, validated semantic enrichment/synthesis, and Gateway/Writer knowledge production.', sections: synthesis.sections.map((section) => ({ id: section.id, title: section.title, markdown: section.items.map((item) => `### ${item.headline}\n\n${item.markdown}`).join('\n\n'), sourceRefs: [], signalRefs: section.items.flatMap((item) => item.signalRefs), evidenceLinks: section.items.flatMap((item) => item.signalRefs).map((signalId) => enriched.find((signal) => signal.signalId === signalId)?.source.url).filter((url): url is string => Boolean(url)) })), outputPath: `${id}.md` }; validateResearchReport(report); await writeResearchReport(report, resolve(input.reportRoot)); await store.put(brief); return { workflowRunId: input.workflowRunId, status: 'completed', brief, reportPath: join(resolve(input.reportRoot), `${id}.md`), signals: enriched, clusters, assessments, providerOutcomes: outcomes, proposalCount: synthesis.proposals.length, committedKnowledgeRefs: knowledge.committedRefs, reviewCaseCount: knowledge.reviewCount, errors: [] }
  } catch (error) { const message = error instanceof Error ? error.message : String(error); return failure(input, message === 'WORKFLOW_CANCELLED' ? 'cancelled' : 'failed', [message]) }
}
function classify(title: string, kind: DailyResearchSignal['kind']): DailyResearchSignal['category'] { if (kind === 'announcement') { if (/预告|预增|预减|业绩快报/.test(title)) return 'performance_forecast'; if (/年报|半年报|季报|业绩报告/.test(title)) return 'earnings'; if (/调研|电话会|路演|投资者关系/.test(title)) return 'investor_relations'; if (/指引|guidance/i.test(title)) return 'management_guidance'; return 'announcement' } if (kind === 'institutional_view') return 'institutional_research'; if (kind === 'community') return 'community'; if (kind === 'market') return 'market'; return 'news' }
function eventKey(title: string, entities: readonly string[], category: string): string { return `event:${hashKnowledgeObject({ title: title.toLowerCase().replace(/宣布|计划|拟|将|关于/g, '').replace(/扩产|扩大产能|增加产能/g, 'capacity-expansion').replace(/\s+/g, ' ').trim(), entities: [...entities].sort(), category }).slice(7, 23)}` }
function identity(signal: DailyResearchSignal): string { return String(signal.source.metadata?.providerObjectId ?? `${signal.sourceAccountRef ?? signal.provider}|${signal.source.url ?? signal.contentHash}|${signal.title}`) }
function shiftDate(date: string, days: number): string { const value = new Date(`${date}T12:00:00+08:00`); value.setUTCDate(value.getUTCDate() + days); return value.toISOString().slice(0, 10) }
async function projectExisting(input: DailyIntelligenceInput, clusters: readonly DailySignalCluster[]): Promise<ReadonlyMap<string, readonly Record<string, unknown>[]>> { const result = new Map<string, readonly Record<string, unknown>[]>(); if (!input.knowledgeBaseRoot) return result; const handle = await new KnowledgeBaseRegistry().mount(resolve(input.knowledgeBaseRoot)); if (handle.schemaVersion !== '0.4' || handle.storageFormatVersion !== '1') return result; const gateway = new KnowledgeProductionGateway(); for (const symbol of new Set(clusters.flatMap((cluster) => cluster.entities.filter((entity) => /^\d{6}$/.test(entity))))) result.set(symbol, await gateway.projectExistingKnowledge(handle, { symbol })); return result }
function assess(cluster: DailySignalCluster, projections: ReadonlyMap<string, readonly Record<string, unknown>[]>): ResearchChangeAssessment { const existing = cluster.entities.flatMap((entity) => projections.get(entity) ?? []); const refs = [...new Set(existing.map((item) => String(item.canonicalRef ?? '')).filter(Boolean))]; const text = cluster.signals.map((signal) => `${signal.title} ${signal.excerpt ?? ''}`).join(' '); const disposition: ResearchChangeAssessment['disposition'] = existing.some((item) => String(item.statement ?? '') && text.includes(String(item.statement))) ? 'supports' : /风险|下滑|下降|减产|亏损|处罚|诉讼/i.test(text) ? 'risk' : /订单|扩产|增长|突破|合作|利好/i.test(text) ? 'catalyst' : refs.length ? 'affects_thesis' : cluster.signals.every((signal) => signal.kind === 'community') ? 'noise' : 'new'; return { clusterId: cluster.clusterId, disposition, rationale: `${cluster.title} is supported by ${cluster.sourceDiversity} provider(s); disposition=${disposition}.`, relatedKnowledgeRefs: refs.slice(0, 20), durableCandidate: refs.length > 0 && disposition !== 'noise' && cluster.signals.some((signal) => signal.sourceTier <= 2) } }
async function projectKnowledge(input: DailyIntelligenceInput, sources: readonly NormalizedResearchSource[], proposals: readonly DailySynthesisProposal[]): Promise<{ proposalCount: number; claimCount: number; rawClaimCount: number; committedRefs: readonly string[]; reviewCount: number; revision?: number }> { if (!input.knowledgeBaseRoot || !proposals.length) return { proposalCount: proposals.length, claimCount: 0, rawClaimCount: 0, committedRefs: [], reviewCount: 0 }; const company = input.watchlist.companies.find((item) => item.symbol === proposals[0]!.subjectKey); if (!company) return { proposalCount: proposals.length, claimCount: 0, rawClaimCount: 0, committedRefs: [], reviewCount: 0 }; const handle = await new KnowledgeBaseRegistry().mount(resolve(input.knowledgeBaseRoot)); if (handle.schemaVersion !== '0.4' || handle.storageFormatVersion !== '1') return { proposalCount: proposals.length, claimCount: 0, rawClaimCount: 0, committedRefs: [], reviewCount: 0, revision: handle.revision }; const bounded = proposals.filter((item) => item.subjectKey === company.symbol).slice(0, input.maxDurableKnowledgeProposals ?? 5); const semantic: SemanticProductionProposal[] = bounded.map((item) => ({ proposalId: item.proposalId, kind: 'claim', subjectKey: item.subjectKey, claimType: item.claimType === 'risk' ? 'risk' : item.claimType === 'trend' ? 'trend' : 'viewpoint', statement: item.statement, sourceCandidateIds: item.sourceCandidateIds, confidence: item.confidence })); const result = await new KnowledgeProductionGateway().submit({ handle, producerType: 'daily_intelligence', producerRunId: input.workflowRunId, schemaProfile: { schemaVersion: '0.4', storageFormatVersion: '1', requiresRawProvenance: true }, entity: { localKey: company.symbol, entityType: 'company', name: company.name ?? company.symbol, semanticFields: { ticker: company.symbol, ...(company.exchange ? { exchange: company.exchange } : {}) } }, proposals: semantic, evidenceBindings: sources.map((source) => ({ localSourceId: source.candidate.candidateId, source })), asOf: input.asOf, now: input.now }); const claims = Object.keys(result.claimRefsByProposalId).length; return { proposalCount: proposals.length, claimCount: claims, rawClaimCount: sources.some((source) => source.rawBytes?.byteLength) ? claims : 0, committedRefs: [...result.createdIds, ...result.updatedIds], reviewCount: result.resolutionIntents.filter((item) => item.disposition === 'review_required').length, revision: result.knowledgeBaseRevision } }
function makeQuality(input: readonly DailyResearchSignal[], deduped: readonly DailyResearchSignal[], clusters: readonly unknown[], ranked: readonly DailyResearchSignal[], sections: readonly { items: readonly { sourceRefs: readonly string[]; signalRefs?: readonly string[] }[] }[], assessments: readonly ResearchChangeAssessment[], storedSignalCount: number, semanticProposalCount: number, committedClaimCount: number, rawClaimCount: number) { const items = sections.flatMap((section) => section.items); return { inputSignalCount: input.length, dedupSignalCount: deduped.length, clusterCount: clusters.length, rankedCount: ranked.length, topCount: ranked.length, claimCount: committedClaimCount, claimWithSourceCount: committedClaimCount, claimWithRawProvenanceCount: rawClaimCount, sourceCount: input.length, uniqueSourceCount: new Set(input.map((signal) => signal.sourceAccountRef ?? signal.provider)).size, unsupportedProposalCount: 0, reportItemWithSourceRatio: items.length ? items.filter((item) => item.sourceRefs.length || (item.signalRefs?.length ?? 0) > 0).length / items.length : 0, durableChangeCount: assessments.filter((item) => item.durableCandidate).length, reviewCount: 0, storedSignalCount, reportItemCount: items.length, semanticProposalCount, committedClaimCount } }
function failure(input: DailyIntelligenceInput, status: DailyIntelligenceResult['status'], errors: readonly string[]): DailyIntelligenceResult { return { workflowRunId: input.workflowRunId, status, signals: [], clusters: [], assessments: [], providerOutcomes: [], proposalCount: 0, committedKnowledgeRefs: [], reviewCaseCount: 0, errors } }
