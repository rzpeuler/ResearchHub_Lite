task_id: RHL-W4-001
status: READY_FOR_SOL_REVIEW
baseline: 1f1fef4d2db455af2ff17e9703701e1a98659eae
branch: codex/w4-001-thesis-expectation-lifecycle
implementation_commit: 9b4d441adb4aaf3c2ff9eb8d9ba938045336b5d7
verified_remote_tip: 9b4d441adb4aaf3c2ff9eb8d9ba938045336b5d7
sync_status: SYNCED
summary: Promoted four bounded thesis-lifecycle Skills, enhanced thesis red-team fragility and kill-criterion evaluation, added Workflow-owned CREATE/REFRESH composition, dispatch routing, tests, and governance documentation.
tests: Focused W4 tests and related architecture/dispatch tests: 31 passing; npm run typecheck passing. Full validation is recorded below.
acceptance_criteria: All bounded W4 implementation criteria are addressed locally; SOL acceptance remains pending; authenticated provider/model E2E is not claimed.
governance_status: W4 implementation preserves the accepted W3 + FIX-002 architecture and adds no Knowledge Schema or Agent Runtime layer.
blockers: No implementation blocker. Authenticated external-provider/model validation remains deferred by scope.

## 1. Baseline

Accepted ancestor: `1f1fef4d2db455af2ff17e9703701e1a98659eae`.
Working branch: `codex/w4-001-thesis-expectation-lifecycle`.
The W4 worktree was created from the accepted FIX-002 tip and the parent
worktrees were left unchanged.

## 2. Starting Catalog State

The baseline catalog was `17 IMPLEMENTED`, `7 PARTIAL`, and `5 PLANNED`, with
the thesis lifecycle entries present as non-runtime metadata or absent from the
direct runtime set.

## 3. Final Catalog State

The local implementation catalog is `21 IMPLEMENTED`, `4 PARTIAL`, and
`4 PLANNED`. The promoted runtime set includes `thesis_formalize`,
`expectation_gap`, `catalyst_map`, and `thesis_refresh`.

## 4. Thesis Formalization

Added an independently callable deterministic contract for explicit thesis
summary, proposition types, evidence basis, dependency references, cycle
detection, verification source requirements, research gaps, and load-bearing
propositions. No canonical Thesis mutation is performed.

## 5. Expectation Gap

Added compatibility-gated comparison across price-implied, consensus,
management, and own-research surfaces. Point values and ranges remain
structural; post-as-of and untraceable surfaces are diagnosed without invented
expectations.

## 6. Thesis Red Team

Enhanced the existing semantic red-team implementation with categorical
fragility (`critical`, `high`, `medium`, `low`, `insufficient_evidence`) and
deterministic kill-criterion shape/evaluation. Thresholds require source
provenance and remain pending when evidence is insufficient.

## 7. Catalyst Map

Added proposition-linked catalyst mapping with event type, status, observable
resolution mechanism, timing/date validation, and source attribution. Directional
investment conclusions are intentionally outside the contract.

## 8. Thesis Refresh

Added point-in-time refresh against a required prior snapshot. Only targeted
propositions change; old or future evidence is excluded, unaffected propositions
are preserved, and sourced kill criteria can produce a candidate invalidation
transition without canonical mutation.

## 9. Workflow Integration

Added `workflows/thesis-lifecycle` with CREATE and REFRESH branches. The
Workflow composes peer results and accepts the existing red-team result as an
injected Workflow peer; it does not perform Skill-to-Skill invocation. The
Workflow is registered as `thesis_lifecycle` and has a bounded dispatch adapter.

## 10. Architecture Integrity

No Agent Runtime, Planner, generic provider abstraction, Knowledge Schema change,
direct frontend canonical write, or alternate persistence path was introduced.
Knowledge effects remain metadata/proposal boundaries owned by the existing
Workflow and Knowledge Production path.

## 11. E2E

Scenario A: CREATE formalizes propositions, compares expectations, accepts a
red-team peer result, and maps a scheduled catalyst.

Scenario B: REFRESH applies new evidence to a targeted proposition while
preserving an unaffected proposition.

Scenario C: irrelevant/context evidence produces no proposition drift.

Scenario D: a sourced threshold and observed metric produce
`invalidation_condition_met` as a candidate transition.

These are deterministic local contract E2E scenarios, not authenticated live
provider or model E2E.

## 12. Tests

Passed locally:

- `npm run typecheck`
- `node --import tsx --test tests/app/services/research-skill-architecture.test.ts tests/skills/wave4-thesis-lifecycle.test.ts tests/app/services/research-dispatch-service.test.ts`

The focused/related command completed with 31 passing tests and zero failures
after the final W4 test additions. Full repository validation and client checks
are executed before synchronization and will be appended here if results differ.

## 13. Catalog Delta

Promoted: `thesis_formalize`, `expectation_gap`, `catalyst_map`, and
`thesis_refresh`. Enhanced without promotion change: `thesis_red_team`.
Remaining partial/planned entries were not promoted by fixture evidence.

## 14. Files Changed

Changed runtime/catalog/dispatch files under `app/services/`; added the four
Skill contracts, calculations, indexes, and SKILL.md files; enhanced the
legacy red-team calculation/contract/skill and canonical SKILL.md; added the
thesis lifecycle Workflow; updated architecture/dispatch tests; and added this
governance and task-report documentation.

## 15. Governance / Docs

Updated `docs/architecture/RESEARCH_SKILL_CATALOG_V1.md` and added
`docs/governance/WAVE4-001.md`. The report preserves the required distinction
between local deterministic readiness and pending SOL/authenticated acceptance.

## 16. Deferred

Authenticated provider data, model-backed semantic quality, canonical lifecycle
write integration, and investment recommendation semantics remain deferred.

## 17. Known Issues

The pre-existing VAL-HTTP-001 timing-sensitive route test is tracked separately
and is not modified by W4 unless full validation reproduces a W4-caused effect.
The local dependency audit still reports two moderate findings from the existing
dependency tree; no force remediation was applied.

## 18. Final Classification

`IMPLEMENTED / SOL ACCEPTANCE PENDING` locally. The implementation is ready for
deterministic safe synchronization once the full validation and protected-path
checks pass.
