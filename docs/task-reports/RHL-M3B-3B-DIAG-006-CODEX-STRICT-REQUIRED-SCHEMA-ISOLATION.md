# RHL-M3B-3B-DIAG-006 — Codex strict-required schema isolation

Status: TEST evidence only. No production change is authorized by this diagnostic.

This report is generated from the bounded validation script and its evidence JSON. DIAG-005 is accepted as valid TEST evidence at `f7d9b4cfe840d6c1fd9cce3d4abf6b0df2e3e6c7`: its scoped tests and adapter/Industry/Pi/typecheck/build checks passed; unrelated timing/Windows cleanup-sensitive aggregate failures remain evidence only. DIAG-005 established that the production Codex/Pi adapter can execute the explicit-typed minimal control, while the type-inferred neutral Design contract started and exited non-zero as `unknown_nonzero_exit`. Its primitive additions were exactly `$.properties.targetKind`, `$.properties.knownGaps.items.properties.module`, and `$.properties.verificationCandidates.items.properties.kind`, normalized fingerprint `66ca1308a52ea98d` at 2159 bytes, with authoritative source fingerprint `9854f93073c44d96`.

The validation script uses only `createCodexCliLunaReasoningExecutor()` and the existing PiReasoningExecutor path, with backend `codex-cli`, model `gpt-5.6-luna`, medium effort, semantic Context via stdin, ephemeral read-only execution and bounded cleanup. Prompts and inputs are neutral and contain no PCB, securities, provider-block, or private-data semantics.

The actual run made three calls. Probe A (5.5s) started and exited non-zero with `unknown_nonzero_exit`; Probe B (10.1s) succeeded with valid `status` and `note`; Probe C (22.2s) succeeded with valid neutral Design output, parser PASS, validator PASS, `targetKind=uncertain`, and eight module questions. Probe D was correctly not attempted. Probe sequence, call count, classifications, normalized schema fingerprints/bytes, safe failure fields, parser/validator status, and bounded result metadata are recorded in [the evidence file](../../tests/validation/evidence/RHL_M3B_CODEX_CLI_STRICT_REQUIRED_SCHEMA_ISOLATION.json). Probe A is the explicit-typed optional-property control (`status` required; `note` optional). Probe B is structurally identical with both properties required. Design Probe C applies only recursive requiredness; Probe D is conditional and applies DIAG-005 primitive inference plus recursive requiredness from a fresh source copy.

Offline coverage verifies recursive schema-position traversal, source immutability and fingerprint stability, DIAG-005 inference reuse, control structural difference, decision early stopping, semantic-invalid stopping, backend stopping, and the four-call upper bound. The real run must be executed with `node --import tsx tests/validation/codex-cli-strict-required-schema-isolation.ts`; normal repository tests do not invoke Codex or the network.

Mutation and privacy assertions cover no production, adapter, Pi executor, normalizer, model-selection, PCB, Industry Skill real invocation, Knowledge/Gateway/Writer/Report/graph mutation and no raw prompts, process streams, complete outputs/schemas, credentials, private paths, or reasoning traces.

Validation status: scoped DIAG-006 tests 5/5 PASS; Codex adapter tests 7/7 PASS; Industry Skill tests 15/15 PASS; Pi gate 19/19 PASS; typecheck PASS; client typecheck PASS; client build PASS; diff check PASS. `npm run test:node` and `npm test` each had 728/729 PASS with the single pre-existing timing/Windows-cleanup-sensitive failure `tests/app/runtime/valuation-route.test.ts` (`running` vs `blocked`); this is evidence-only under the TEST contract and was not changed. No commit, push, amend, rebase, or force-push is performed by Luna.
