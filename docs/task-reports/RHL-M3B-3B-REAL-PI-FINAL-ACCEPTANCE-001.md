# RHL-M3B-3B-REAL-PI-FINAL-ACCEPTANCE-001

Status: `REAL_MODEL_CONTRACT_BLOCKED / CTO REVIEW REQUIRED`

## Scope and baseline

This was an acceptance TEST task at M3B-3A CTO PASS baseline `2e265601b1d57f87e61398a50743e6fb760c09a4`. No production code, governance, architecture, Knowledge, Skill, Workflow, Plugin, or application files were modified. The validation additions are the pure gate, unit tests, bounded fixture corpus, real-Pi harness, and this report.

## Corpus boundary

The bounded corpus `pcb-a-share-ai-server-hdi-v1` represents an A-share-relevant PCB manufacturing case with AI-server and HDI context, an upstream copper-clad-laminate chain fact, an official-disclosure-style A-share company exposure, and qualitative risk material. It contains no unsupported quantitative market-size, growth, capacity, price, margin, utilization, penetration, or share values. Sentinel leakage testing was included. These are retained validation inputs only; they are not proof of live GDELT, AKShare, CNINFO, or RSS success. M3B-3A public-provider smoke remains the authority and recorded current free-provider availability as degraded/empty.

## Real Pi result

The harness used the configured local Pi `ModelRuntime`, production model-selection path, and actual `PiReasoningExecutor` with bounded timeout/output settings. Sanitized runtime metadata selected `zhipu-openapi/glm-5.3-flash` through provider `pi-coding-agent`. The observation wrapper recorded only operation names/counts and retained semantic shapes in memory. The run reached `industry_research_design` twice (the model/contract retry path), then failed before module execution. No completed Workflow, eight-module result, canonical first-run revision, durable Claim/Relation/Company mapping, sixteen-section report acceptance, or canonical industry graph acceptance was established.

The exact failed predicates are preserved in [RHL_M3B_INDUSTRY_RESEARCH_PI_E2E.json](../../tests/validation/evidence/RHL_M3B_INDUSTRY_RESEARCH_PI_E2E.json), including missing bounded operation coverage, incomplete module result, missing canonical objects, report/graph gates, and replay gate failure. This is model/contract evidence for separately scoped review; no production repair was attempted.

## Replay and privacy

The harness contains a separate producer-neutral Gateway replay path with a different producer run identifier and compares canonical IDs/counts, revision, raw provenance, and graph identity. Because the first stochastic run did not satisfy the frozen completed-run acceptance, the replay branch was correctly skipped and recorded as blocked; no second stochastic model run occurred. The persisted evidence contains bounded metadata, hashes/IDs/counts, sanitized diagnostics, and booleans only. It does not contain complete prompts, model outputs, fixture source bodies, authentication content, cookies, authorization headers, absolute temporary paths, or secrets; the unique fixture sentinel is absent.

## Validation

- Gate unit tests: passed (17 tests).
- M3B-3A deterministic Industry Application integration: passed (2 tests).
- Real Pi acceptance: executed; contract blocked, exit code 1.
- Full repository validation and client/build checks are recorded by the orchestrator after this report; the real model command remains explicitly invoked and is not an `npm test` dependency.

Final classification: `REAL_MODEL_CONTRACT_BLOCKED / CTO REVIEW REQUIRED`.
