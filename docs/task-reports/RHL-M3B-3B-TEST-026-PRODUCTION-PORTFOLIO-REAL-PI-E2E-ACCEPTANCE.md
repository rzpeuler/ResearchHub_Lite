# RHL-M3B-3B TEST-026 — Production portfolio real Pi E2E acceptance

## Result

The TEST-026 entrypoint and deterministic offline classifier were added within the test-only boundary. The live command writes sanitized evidence to `tests/validation/evidence/RHL_M3B_INDUSTRY_PRODUCTION_PORTFOLIO_REAL_PI_E2E_ACCEPTANCE.json`. The final classification is recorded in that evidence file; external model/provider setup failures are classified `PORTFOLIO_E2E_LIVE_INCONCLUSIVE` without retry or configuration changes.

## Baseline and method

Baseline: `805e3db41b329eb18ba174716f5c28ff6a5cbcbf`. Target: PCB Manufacturing, aliases Printed Circuit Board and 印制电路板, as-of `2026-09-14T00:00:00.000Z`, with the eight frozen search terms in the entrypoint. A fresh temporary Schema 0.4 / Storage Format 1 KB and report directory are used. The run uses the production Pi/Codex selection path and one live Workflow execution, followed by one replay from captured normalized sources and structured reasoning outputs.

The instrumented production order is CNINFO, GDELT, MIIT, Gov.cn, Eastmoney, CPCA, then AKShare appended by `ResearchService`, matching the current application composition. Instrumentation records metadata only; it does not change provider behavior.

## Audits and validation

Evidence records provider attempts/outcomes, source tiers/hosts/date availability/hashes, reasoning operation counts, module status, bounded waves, Gateway/Writer/revision counts, canonical counts, Raw provenance, numeric audit, graph/report summaries, replay counters and duplicate audit. No raw bodies, complete prompts/outputs, reasoning traces, credentials, cookies, authorization headers, or private paths are persisted.

The exact live results, report section audit, Knowledge/provenance audit, replay/idempotency result, validation command statuses, residual gaps, and final classification are authoritative in the JSON evidence artifact generated by the live command. If the run is partial, unavailable evidence remains explicit in Research Gaps and is not converted into durable facts.

Validation commands:

- `npx tsx --test tests/validation/industry-production-portfolio-real-pi-e2e-acceptance.test.ts`
- `node --import tsx tests/validation/industry-production-portfolio-real-pi-e2e-acceptance.ts`
- `npx tsx --test tests/validation/industry-research-pi-e2e-gate.test.ts`
- `npm run typecheck`
- `npm test`
- `git diff --check`

## Recommended next step

Accept or route this milestone to product-quality review based on the final classification in the evidence file; do not add source plumbing unless the observed evidence proves a specific critical source deficiency.
