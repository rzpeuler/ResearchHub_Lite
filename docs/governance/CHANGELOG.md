# ResearchHub_Lite Changelog

## 2026-09-08

- CTO accepted `RHL-PERSONAL-RESEARCH-V1-DAILY-INTELLIGENCE-001`, `...-FIX-001`, `...-FIX-002`, and `...-FIX-003` as `PASS / CLOSED` on 2026-09-08 at accepted implementation commit `218280579cdd7b6cfdf67506150cfa82cef4322e`. M2 is merged to `main`; M3 was not started.

- Final local validation record: FIX-003 focused executable suite `7/7 PASS`; FIX-003 acceptance requirement matrix `12/12 PASS`; Full Node `348/348 PASS`; Client `21/21 PASS`; `typecheck`, `client:typecheck`, `client:build`, and `git diff --check` PASS. Real Pi used `zhipu-openapi/glm-5.3-flash`; Morning and Evening completed with `gate=true`, enrichment `6/0`, Change Assessment `4/2`, and Synthesis `6/0` for applied/fallback counts. No GitHub CI status is claimed.

- Remaining non-blocking risks are free public provider availability/HTTP reliability, limited automatic active-source coverage, model semantic quality variance, and deterministic fallback when semantic output is rejected. These do not reopen M2.

- Implemented `RHL-PERSONAL-RESEARCH-V1-DAILY-INTELLIGENCE-001-FIX-003`: bounded serialization normalization for the three Daily Intelligence reasoning operations, partial-field enrichment fallback, Change Assessment durable eligibility authority for Synthesis proposals, exact section admissibility, executable multi-company/evidence regressions, and sanitized real-Pi Product telemetry/evidence.

- Implemented `RHL-PERSONAL-RESEARCH-V1-DAILY-INTELLIGENCE-001`: Morning/Evening Brief contracts and sections, 43-entry public source catalog, explicit watchlist, bounded Web/Institutional/Community acquisition seams, ResearchSignal v2 store/dedup/enrich/cluster/rank, report persistence, trading-day scheduler, CLI/Task Scheduler scripts, Pi tools, HTTP routes, client API/card surface, offline regression tests, live provider probes, and real PiReasoningExecutor Morning/Evening evidence.

- Implemented `RHL-PERSONAL-RESEARCH-V1-DAILY-INTELLIGENCE-001-FIX-002`: asynchronous enrichment application, bounded four-state change assessment, strict proposal/evidence admissibility, per-company Gateway grouping, canonical-only report subjects, shared Runtime/CLI/Scheduler/Pi composition, active-only automatic catalog wiring, manual calendar precedence, and real product telemetry/evidence harnesses.

- Implemented `RHL-PERSONAL-RESEARCH-V1-FOUNDATION-001-FIX-001` pending independent CTO acceptance: Company Research now emits local semantic proposals to a producer-neutral Knowledge Production Gateway; canonical identity, Knowledge State Diff, bounded ResolutionIntent policy, ChangeSet planning/validation, and one shared Writer authority are centralized. Raw evidence uses the existing archive/verification seam, V04 receipts carry runtime Validator identity, temporal/as-of filtering and source rights policy are explicit, report retrieval is available through Application API/Pi tool, and offline plus real-network multi-run regressions are recorded in `tests/validation/evidence/RHL_PERSONAL_RESEARCH_V1_FOUNDATION_FIX_001.*`.
- Added Personal Research v1 Company Deep Research foundation: Schema 0.4 contracts and validation, bounded official/AKShare/GDELT/RSS acquisition, ResearchSignal and Markdown ResearchReport contracts, deterministic valuation utilities, Company Research Skill, Company Deep Research Workflow, and `research_company` Application Service/Product API integration. Schema 0.3 runtime data and Raw Document Ingestion remain unchanged.

- CTO accepted `RHL-MIGRATE-REPOSITORY-LAYOUT-PHASE-B-001` as `PASS / CLOSED`. Repository Layout v1 physical migration through Phase B is complete; Phase C is `NOT STARTED / DEFERRED` with no automatic Phase C authorization. Production Baseline v1 remains unchanged, and the next active focus is Knowledge Production / Knowledge Architecture iteration.
- Executed `RHL-MIGRATE-REPOSITORY-LAYOUT-PHASE-B-001`: moved Workflow internals into planning/extraction/resolution/review/changeset/shared, Knowledge Curation internals into identity/model/validation, and Document Plugin parsers into `parsers/text/` and `parsers/docling/`. Updated only import/export paths and preserved all root public entrypoints; Phase A and its FIX are recorded as PASS / CLOSED by CTO decision, and Phase C remains unstarted.
- CTO accepted `RHL-MIGRATE-REPOSITORY-LAYOUT-PHASE-A-001` and `RHL-MIGRATE-REPOSITORY-LAYOUT-PHASE-A-001-FIX-001` as PASS / CLOSED; their historical commits and scope remain unchanged.
- Executed `RHL-MIGRATE-REPOSITORY-LAYOUT-PHASE-A-001-FIX-001`: resolved the Workflow public barrel `EntityResolution` collision with explicit planner exports, removed the root TypeScript exclusion for the Workflow barrel, and made `tests/validation/evidence/` an explicit Node discovery boundary with one deterministic infrastructure regression test. Root/client typechecks and the existing suites remain green; Phase A remains CTO acceptance pending.
- Recorded `RHL-FIX-REVIEW-TERMINAL-CASE-INVARIANT-001` as `PASS / CLOSED by CTO decision`; its historical real reproduction and immutable evidence remain unchanged.
- Executed `RHL-VALIDATE-RUNTIME-RESTART-PERSISTENCE-001` with a real `zhipu-openapi/glm-5.3-flash` Production fixture, Docling 2.116.0, Runtime A hard close, and a fresh Runtime B. Canonical, Raw, Knowledge API, Graph, and deterministic actionable ReviewCase persistence all passed with unchanged revision and no post-restart writes. Evidence is preserved in `tests/validation/evidence/rhl-runtime-restart-persistence-001.json` and `tests/validation/evidence/RHL_RUNTIME_RESTART_PERSISTENCE_001_SUMMARY.md`; classification is `SUCCESS`, CTO acceptance pending, and no full Production E2E or Free Research was run.
- CTO decision established `ResearchHub Lite Production Baseline v1` from the accepted compositional validation chain. `RHL-VALIDATE-RUNTIME-RESTART-PERSISTENCE-001` is recorded as `PASS / CLOSED`; the baseline freezes the accepted Pi/provider, Application, Knowledge Production, persistence, read-model, security, and frontend scope without changing frozen architecture. The next phase is Knowledge Production / Knowledge Architecture iteration; no automatic Production E2E rerun, provider architecture work, Runtime refactor, or harness expansion is authorized.
- Recorded `RHL-FREEZE-REPOSITORY-LAYOUT-V1-001` as `FROZEN / CTO decision` in `docs/architecture/RESEARCHHUB_LITE_REPOSITORY_LAYOUT_V1.md`. The eight root directories, ownership/dependency rules, tests mirror rule, immutable validation evidence path, and three migration phases are now governed; no Phase A/B/C migration or source-code change was performed.
- Executed `RHL-MIGRATE-REPOSITORY-LAYOUT-PHASE-A-001`: moved ordinary tests with `git mv` to mirror source ownership, moved the engineering spec to `docs/engineering/specs/`, added Windows-safe deterministic Node test discovery, included Workflow TypeScript ownership, and preserved validation evidence and production semantics. The suite remains client `21/21` and Node `298/298`; CTO acceptance is pending.

## 2026-09-07

- Implemented `RHL-FIX-REVIEW-TERMINAL-CASE-INVARIANT-001`: Workflow terminal status and persisted `workflowStatusHint` now use durable actionable ReviewCase count rather than `ReviewSummary.total`; Replay normalizes historical completed success status from recovered cases without rewriting logs; ProductionService rejects inconsistent success status/count pairs; added offline regression and validation-harness classification consistency coverage. CTO acceptance is recorded as `PASS / CLOSED by CTO decision`.
- Executed the targeted real Production validation with real `zhipu-openapi/glm-5.3-flash`, PiReasoningExecutor, Docling 2.116.0, fresh Knowledge, and real Attachment/Production/Review APIs. The result was `completed`, `reviewCount=0`, Review API HTTP 200 with no current producer case, ingestion log `completed`, and canonical revision 1. Evidence and summary are preserved under `tests/validation/evidence/rhl-review-terminal-case-invariant-001.*`; CTO acceptance remains pending. Runtime B restart, browser smoke, and full E2E were not run.
- Implemented `RHL-FIX-ATTACHMENT-PRODUCTION-PATH-BOUNDARY-001`: AttachmentService now provides a validated workspace-relative production reference after `readAndValidate()`, Runtime Server uses that reference, and the existing canonical `resolveAttachmentPath()`/`openAttachment()` behavior and ProductionService security boundary remain unchanged. Added DTO privacy, metadata-integrity, Runtime ingress, and Windows canonical/lexical representation regressions.
- Executed the targeted real reproduction with `zhipu-openapi/glm-5.3-flash`, real PiReasoningExecutor, real Docling 2.116.0, fresh KB, and real Attachment/Production APIs. The reference was relative and resolved inside Runtime/Production workspace and outside canonical Knowledge; Workflow completed with review, Raw archive integrity passed, ingestion log committed revision 1, and no new authoritative failure. Full Production E2E was not run; CTO acceptance is pending.
- Updated the validation diagnostic to distinguish the internal canonical attachment path from the production workspace-relative reference. Historical diagnosis evidence remains immutable; `RHL-DIAGNOSE-PRODUCTION-WORKFLOW-FAILURE-001` is recorded as PASS / CLOSED by CTO decision.
- Corrected the validation-only Production E2E harness to persist safe terminal Workflow evidence before terminal classification and to validate the public Attachment DTO without requiring internal workspace paths. Added deterministic regression coverage; no Production implementation changed.
- Executed `RHL-VALIDATE-PRODUCTION-E2E-001-RERUN-003` against a fresh KB with real `zhipu-openapi/glm-5.3-flash`, Pi ModelRuntime/PiReasoningExecutor, Docling 2.116.0, Runtime HTTP/SSE, and the full Attachment -> Production -> Knowledge path. The run classified `SUCCESS`: Free Research, attachment privacy/boundary, `completed_with_review` Production Workflow, real reasoning (3/3), Docling, Raw archive, Writer revision 0→1, canonical/provenance/transient-reference checks, Knowledge/Graph/Review APIs, Edge/CUA browser smoke, exact replay, and loopback security all passed. Replay preserved revision/counts with no duplicate canonical content. Evidence is preserved in `tests/validation/evidence/rhl-production-e2e-001-rerun-003.json` and `tests/validation/evidence/RHL_PRODUCTION_E2E_001_RERUN_003_SUMMARY.md`; CTO acceptance remains pending. The browser-harness CLI was unavailable because the local `browser_harness` module was missing, so the required three-page smoke used the connected Edge/CUA fallback and was recorded explicitly.
- Governance interpretation for RERUN-003 is corrected to `VALIDATION_HARNESS_DEFECT / CTO reviewed` without changing its raw evidence: its Stage N replay used the same Runtime and a second Production workflow, so it proved duplicate-ingestion canonical stability rather than Runtime restart persistence; Review UI visibility passed, but Review API list/detail lacked independent evidence.
- Executed `RHL-VALIDATE-RESTART-REVIEW-PERSISTENCE-001` with two fresh real fixtures using `zhipu-openapi/glm-5.3-flash`, Pi ModelRuntime/PiReasoningExecutor, and Docling 2.116.0. The first terminal was `completed` with no ReviewCase; the one permitted retry returned `completed_with_review` with `reviewCount=0`, while `GET /api/reviews` returned HTTP 200 with no case for the current producer run. Runtime A reached revision 1 with valid Raw, Canonical, Knowledge API, and Graph snapshots, but the task stopped at the real Review terminal/list inconsistency before Runtime B restart. Classification: `PRODUCT_DEFECT / CTO acceptance pending`; no Production implementation was modified.

- Corrected the CTO-reviewed classification of `RHL-VALIDATE-PRODUCTION-E2E-001-RERUN-001` to `VALIDATION_HARNESS_DEFECT`: its Free Research oracle incorrectly required a fixed marker in serialized SSE after a valid assistant completion. The old evidence remains immutable; no production defect was established.
- Added the validation-only Free Research oracle and deterministic regression coverage, and prepared independent `RHL-VALIDATE-PRODUCTION-E2E-001-RERUN-002` evidence paths. The corrected oracle validates lifecycle completion, safe normalized SSE, assistant deltas, persisted current-request nonce, and non-empty persisted assistant content without fixed wording.
- Executed `RHL-VALIDATE-PRODUCTION-E2E-001-RERUN-002` against real `zhipu-openapi/glm-5.3-flash`, real Pi ModelRuntime/PiReasoningExecutor, Docling 2.116.0, and a fresh Knowledge Base. Provider preflights, Application boot, Free Research, attachment upload, and the upload-only canonical-boundary check passed. The real Production Workflow then returned terminal status `failed`; the original evidence did not retain enough diagnostics for root-cause classification, so governance is `VALIDATION_HARNESS_DEFECT / CTO reviewed`. No production implementation was modified after the failure and downstream API, Graph, Review, browser smoke, and replay stages were not run.
- Executed `RHL-DIAGNOSE-PRODUCTION-WORKFLOW-FAILURE-001` with a fresh KB and the real Attachment -> `/api/production/ingest` -> `/api/workflows/:runId` path. The terminal error was safely retained as `workspaceFile must remain inside workspaceRoot and outside the canonical Knowledge Base`; path forensics showed the AttachmentService canonical path differed from the ProductionService lexical boundary on Windows. Failure phase is `INPUT_RESOLUTION`, reasoning calls/Raw/log/canonical revision are all zero, and the root cause is classified `PRODUCT_DEFECT / CTO acceptance pending`. No production fix was implemented.

- `RHL-CONFIGURE-PI-MULTI-PROVIDER-001` is now recorded as `PASS / CLOSED` by CTO decision; both configured provider gates remain cleared.

- Executed `RHL-CONFIGURE-PI-MULTI-PROVIDER-001`: configured local Pi `zhipu-openapi/glm-5.3-flash` and Pi-native `openai-codex` OAuth without writing credentials to the repository. Both providers passed the Native Pi and PiReasoningExecutor gates; the multi-provider result is `MULTI_PROVIDER_GATE_CLEARED / CTO acceptance pending`. The full Production Application E2E remains intentionally not run.

- Implemented Knowledge Graph Projection v0.1 and admitted the frozen architecture artifact byte-for-byte.
- Added bounded deterministic Directory and rooted graph read APIs.
- Replaced the `/graph` placeholder with a read-only React Flow + Dagre page with search, filters, depth, focus/re-root, URL state, and canonical Inspector detail.
- Added service, runtime, client, and regression coverage without changing canonical Knowledge mutation authority.

Graph Page v0.1 status: PASS / CLOSED by CTO decision.

- Executed `RHL-VALIDATE-PRODUCTION-E2E-001` with a validation-only harness and fresh-KB/real-Docling preflight.
- Recorded `ENVIRONMENT_BLOCKED` at real Pi provider completion preflight; no production implementation was modified and no faux/mock component was used to claim success.

Production Application E2E status: ENVIRONMENT_BLOCKED / CTO reviewed; rerun requires official Pi-native authorization for the configured real DeepSeek credential.

- Executed `RHL-DIAGNOSE-PI-PROVIDER-ENVIRONMENT-001` without rerunning full E2E. ModelRuntime exposed `deepseek/deepseek-v4-flash`, but the minimal native Pi completion returned sanitized HTTP 401 `authentication_failed`; PiReasoningExecutor was correctly not run above the failed native layer.
- Added diagnosis evidence and corrected the validation harness to preserve sanitized error categories/codes/status instead of blanket-auth attribution. Graph Page and Graph FIX governance are recorded as `PASS / CLOSED` by CTO decision.
