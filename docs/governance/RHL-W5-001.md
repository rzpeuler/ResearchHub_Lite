# RHL-W5-001 — Comparable Valuation and Cross-Workflow Quality Gate

Status: IMPLEMENTED / SOL ACCEPTANCE PENDING  
Baseline: `d589bf15e9830fe57f73a6fdad9441c50d30abe5`  
Working branch: `codex/w5-001-comps-research-quality`

Wave 5 corrects responsibility ownership and implements the bounded comparable
valuation path. The canonical catalog is 26 entries: 22 `IMPLEMENTED`, 0
`PARTIAL`, and 4 `PLANNED`. The count decreases because three former catalog
entries were not independent Skills: evidence normalization belongs to
acquisition/shared infrastructure, valuation cross-check belongs to the
Valuation Workflow, and research QC belongs to the reusable Workflow-layer
terminal `ResearchQualityGate`.

`comps_valuation` is independently executable through an explicit peer identity,
source, point-in-time, period, unit, currency, comparability, metric-availability,
distribution, and implied-value contract. It reuses the existing comparable
calculation module and never creates synthetic peers.

`ResearchQualityGate` runs before Gateway submission in Valuation, Company,
Earnings, Industry, Event, Thesis Lifecycle, and Daily Intelligence. It is
fail-closed on errors and warning-tolerant for optional unavailable sections.
Valuation cross-check output remains Workflow-owned and exposes method
availability/conflicts without averaging methods.

The four remaining planned entries are `document_change_analysis`,
`earnings_call_analysis`, `financial_model_build_update`, and `model_audit`.
They remain visible in the catalog and are not runtime advertised.
